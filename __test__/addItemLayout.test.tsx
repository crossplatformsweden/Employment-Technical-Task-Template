import React from 'react';

jest.unmock('react-native');

describe('Add Item Function', () => {
    describe('Field Validation', () => {
        test('All empty field',()=> {
            // @ts-ignore
            // expect(AddItemLayout.isSuccessFieldValidation('','').toBe(NAME_VALIDATION_ERROR))
            expect()
        })
    })
})

